<%= form_for deed do |f| %>
  <%= render 'shared/errors', record: deed %>

  <%= f.label :trash_bags %>
  <%= f.number_field :trash_bags, min: 0, step: 1 %>

  <%= f.label :miles %>
  <%= f.number_field :miles, min: 0, step: 1 %>

  <%= f.submit %>
<% end %>
